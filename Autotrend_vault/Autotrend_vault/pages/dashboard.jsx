import { useEffect, useState } from "react"; import io from "socket.io-client"; const socket = io(); export default function Dashboard() { const [events, setEvents] = useState([]); useEffect(() => { socket.on("mutation", data => setEvents(prev => [...prev, data])); }, []); return (<div><h1>Storefront Dashboard</h1><div>{events.map((e, i) => <p key={i}>{e}</p>)}</div><div><h2>Cole</h2><p>Monitoring license gates and ingestion</p><h2>Shadow X</h2><p>Echoing legacy events and bot triggers</p></div></div>); }
